function style(){if(document.querySelector('[data-component-style="search"]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href=new URL('./style/searchBar.css',import.meta.url);l.dataset.componentStyle='search';document.head.append(l)}
export function createSearchBar({placeholder='Search',value='',onSearch=()=>{},delay=250}={}){style();const form=document.createElement('form');form.className='ui-search';form.innerHTML=`<label><span class="ui-search__label">${placeholder}</span><input type="search" value="${value}" placeholder="${placeholder}" autocomplete="off"></label><button type="submit">Search</button>`;const input=form.querySelector('input');let timer;const emit=()=>onSearch(input.value.trim());input.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(emit,delay)});form.addEventListener('submit',e=>{e.preventDefault();clearTimeout(timer);emit()});return {element:form,input,setValue(next){input.value=next}}}
