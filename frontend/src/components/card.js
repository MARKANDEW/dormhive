function style(){if(document.querySelector('[data-component-style="card"]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href=new URL('./style/card.css',import.meta.url);l.dataset.componentStyle='card';document.head.append(l)}
export function createCard({title='',subtitle='',content='',footer='',imageUrl='',className=''}={}){style();const card=document.createElement('article');card.className=`ui-card ${className}`;card.innerHTML=`${imageUrl?`<img class="ui-card__image" src="${imageUrl}" alt="">`:''}<div class="ui-card__content">${subtitle?`<p class="ui-card__subtitle">${subtitle}</p>`:''}${title?`<h3>${title}</h3>`:''}<div class="ui-card__body"></div></div>${footer?'<footer class="ui-card__footer"></footer>':''}`;const body=card.querySelector('.ui-card__body');typeof content==='string'?body.innerHTML=content:body.append(content);if(footer){const f=card.querySelector('footer');typeof footer==='string'?f.innerHTML=footer:f.append(footer)}return card}
